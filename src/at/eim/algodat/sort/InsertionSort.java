package at.eim.algodat.sort;

public class InsertionSort implements Sorter{

    @Override
    public int[] sort(int[] data) {
        int sorted = 0;
        while (sorted != data.length-2) {
            if (data[sorted]>data[sorted+1]) {
                
            }
        }

        return data;
    }

}
